//find the lenght of string manualy
#include<stdio.h>
int main(){
     char name[30];
     int i,j=0;
     printf("\nenter the strig to find it's lenght:");
     gets(name);
     for(i=0;name[i]!='\0';i++)
     {         }
     printf("\n the lenght of string is %d",i);
     return 0;